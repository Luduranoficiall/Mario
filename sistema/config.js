/**
 * Configuração da conexão com o backend (Google Apps Script).
 *
 * Preencha estes dois valores DEPOIS de publicar o Apps Script.
 * Veja o passo a passo em ../backend/INSTRUCOES.md
 *
 * Enquanto WEB_APP_URL estiver vazio, o sistema funciona em
 * MODO DEMONSTRAÇÃO (dados de exemplo), para você ver o visual.
 */
const CONFIG = {
    // Cole aqui a URL do "App da Web" (termina em /exec)
    WEB_APP_URL: '',

    // A MESMA senha que você definiu em API_TOKEN no Codigo.gs
    API_TOKEN: ''
}
